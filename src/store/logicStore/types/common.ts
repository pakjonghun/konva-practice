export type Direction = 'Input' | 'Output';

export type TPinType = 'Parameter' | 'Flow';

export type Position = {
  x: number;
  y: number;
};

export type Size = {
  width: number;
  height: number;
};
